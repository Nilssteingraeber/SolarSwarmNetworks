Status
-----
status_id serial PK
robot_id int FK >- Robot.robot_id
battery "double precision" NULLABLE
cpu_1 "double precision" NULLABLE
point jsonb NULLABLE
orientation jsonb NULLABLE
# UTC-Timestamp in Sekunden als 64-bit Integer
# mit GROUP BY, für neustes Tupel pro Roboter
last_heard bigint DEFAULT=0


Robot
-----
robot_id serial PK
nid varchar(64) UNIQUE
state_id int FK >- State.state_id NULLABLE # BEFORE UPDATE TRIGGER -> INSERT INTO StateChange
display_name varchar(64) NULLABLE
ipv4 varchar(16) NULLABLE # 15 Zeichen für ipv4
ipv6 varchar(40) NULLABLE # 39 Zeichen für ipv6
mac varchar(24) NULLABLE # 17 Zeichen für mac

State
-----
state_id serial PK
state varchar(64) UNIQUE

StateChange
-----
change_id serial PK
state_id int FK >- State.state_id
robot_id int FK >- Robot.robot_id
begin bigint

Neighbor
-----
robot_id int PK FK >- Robot.robot_id
neighbor int PK FK >- Robot.robot_id
strength real DEFAULT=1.0